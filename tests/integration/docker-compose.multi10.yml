services:
  rendezvous:
    build:
      context: ../..
      dockerfile: tests/integration/Dockerfile.multi
    hostname: rendezvous
    networks:
      pilot-p2p:
        ipv4_address: ${PILOT_SUBNET_PREFIX:-172.34.0}.10
    command: >
      pilot-rendezvous
      -registry-addr 0.0.0.0:9000
      -beacon-addr 0.0.0.0:9001
      -http 0.0.0.0:8080
      -log-level info
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:8080/api/stats >/dev/null || exit 1"]
      interval: 2s
      timeout: 2s
      retries: 10

  a1:
    build:
      context: ../..
      dockerfile: tests/integration/Dockerfile.multi
    hostname: a1
    depends_on:
      rendezvous:
        condition: service_healthy
    networks:
      pilot-p2p:
        ipv4_address: ${PILOT_SUBNET_PREFIX:-172.34.0}.21
    environment:
      PILOT_REGISTRY: "${PILOT_SUBNET_PREFIX:-172.34.0}.10:9000"
    command: >
      pilot-daemon
      -registry ${PILOT_SUBNET_PREFIX:-172.34.0}.10:9000
      -beacon ${PILOT_SUBNET_PREFIX:-172.34.0}.10:9001
      -hostname a1 -identity /root/.pilot/identity.json -email a1@p2p.test
      -endpoint ${PILOT_SUBNET_PREFIX:-172.34.0}.21:4000 -listen :4000 -public -keepalive 5s -log-level info

  a2:
    build:
      context: ../..
      dockerfile: tests/integration/Dockerfile.multi
    hostname: a2
    depends_on:
      rendezvous:
        condition: service_healthy
    networks:
      pilot-p2p:
        ipv4_address: ${PILOT_SUBNET_PREFIX:-172.34.0}.22
    environment:
      PILOT_REGISTRY: "${PILOT_SUBNET_PREFIX:-172.34.0}.10:9000"
    command: >
      pilot-daemon
      -registry ${PILOT_SUBNET_PREFIX:-172.34.0}.10:9000
      -beacon ${PILOT_SUBNET_PREFIX:-172.34.0}.10:9001
      -hostname a2 -identity /root/.pilot/identity.json -email a2@p2p.test
      -endpoint ${PILOT_SUBNET_PREFIX:-172.34.0}.22:4000 -listen :4000 -public -keepalive 5s -log-level info

  a3:
    build:
      context: ../..
      dockerfile: tests/integration/Dockerfile.multi
    hostname: a3
    depends_on:
      rendezvous:
        condition: service_healthy
    networks:
      pilot-p2p:
        ipv4_address: ${PILOT_SUBNET_PREFIX:-172.34.0}.23
    environment:
      PILOT_REGISTRY: "${PILOT_SUBNET_PREFIX:-172.34.0}.10:9000"
    command: >
      pilot-daemon
      -registry ${PILOT_SUBNET_PREFIX:-172.34.0}.10:9000
      -beacon ${PILOT_SUBNET_PREFIX:-172.34.0}.10:9001
      -hostname a3 -identity /root/.pilot/identity.json -email a3@p2p.test
      -endpoint ${PILOT_SUBNET_PREFIX:-172.34.0}.23:4000 -listen :4000 -public -keepalive 5s -log-level info

  a4:
    build:
      context: ../..
      dockerfile: tests/integration/Dockerfile.multi
    hostname: a4
    depends_on:
      rendezvous:
        condition: service_healthy
    networks:
      pilot-p2p:
        ipv4_address: ${PILOT_SUBNET_PREFIX:-172.34.0}.24
    environment:
      PILOT_REGISTRY: "${PILOT_SUBNET_PREFIX:-172.34.0}.10:9000"
    command: >
      pilot-daemon
      -registry ${PILOT_SUBNET_PREFIX:-172.34.0}.10:9000
      -beacon ${PILOT_SUBNET_PREFIX:-172.34.0}.10:9001
      -hostname a4 -identity /root/.pilot/identity.json -email a4@p2p.test
      -endpoint ${PILOT_SUBNET_PREFIX:-172.34.0}.24:4000 -listen :4000 -public -keepalive 5s -log-level info

  a5:
    build:
      context: ../..
      dockerfile: tests/integration/Dockerfile.multi
    hostname: a5
    depends_on:
      rendezvous:
        condition: service_healthy
    networks:
      pilot-p2p:
        ipv4_address: ${PILOT_SUBNET_PREFIX:-172.34.0}.25
    environment:
      PILOT_REGISTRY: "${PILOT_SUBNET_PREFIX:-172.34.0}.10:9000"
    command: >
      pilot-daemon
      -registry ${PILOT_SUBNET_PREFIX:-172.34.0}.10:9000
      -beacon ${PILOT_SUBNET_PREFIX:-172.34.0}.10:9001
      -hostname a5 -identity /root/.pilot/identity.json -email a5@p2p.test
      -endpoint ${PILOT_SUBNET_PREFIX:-172.34.0}.25:4000 -listen :4000 -public -keepalive 5s -log-level info

  a6:
    build:
      context: ../..
      dockerfile: tests/integration/Dockerfile.multi
    hostname: a6
    depends_on:
      rendezvous:
        condition: service_healthy
    networks:
      pilot-p2p:
        ipv4_address: ${PILOT_SUBNET_PREFIX:-172.34.0}.26
    environment:
      PILOT_REGISTRY: "${PILOT_SUBNET_PREFIX:-172.34.0}.10:9000"
    command: >
      pilot-daemon
      -registry ${PILOT_SUBNET_PREFIX:-172.34.0}.10:9000
      -beacon ${PILOT_SUBNET_PREFIX:-172.34.0}.10:9001
      -hostname a6 -identity /root/.pilot/identity.json -email a6@p2p.test
      -endpoint ${PILOT_SUBNET_PREFIX:-172.34.0}.26:4000 -listen :4000 -public -keepalive 5s -log-level info

  a7:
    build:
      context: ../..
      dockerfile: tests/integration/Dockerfile.multi
    hostname: a7
    depends_on:
      rendezvous:
        condition: service_healthy
    networks:
      pilot-p2p:
        ipv4_address: ${PILOT_SUBNET_PREFIX:-172.34.0}.27
    environment:
      PILOT_REGISTRY: "${PILOT_SUBNET_PREFIX:-172.34.0}.10:9000"
    command: >
      pilot-daemon
      -registry ${PILOT_SUBNET_PREFIX:-172.34.0}.10:9000
      -beacon ${PILOT_SUBNET_PREFIX:-172.34.0}.10:9001
      -hostname a7 -identity /root/.pilot/identity.json -email a7@p2p.test
      -endpoint ${PILOT_SUBNET_PREFIX:-172.34.0}.27:4000 -listen :4000 -public -keepalive 5s -log-level info

  a8:
    build:
      context: ../..
      dockerfile: tests/integration/Dockerfile.multi
    hostname: a8
    depends_on:
      rendezvous:
        condition: service_healthy
    networks:
      pilot-p2p:
        ipv4_address: ${PILOT_SUBNET_PREFIX:-172.34.0}.28
    environment:
      PILOT_REGISTRY: "${PILOT_SUBNET_PREFIX:-172.34.0}.10:9000"
    command: >
      pilot-daemon
      -registry ${PILOT_SUBNET_PREFIX:-172.34.0}.10:9000
      -beacon ${PILOT_SUBNET_PREFIX:-172.34.0}.10:9001
      -hostname a8 -identity /root/.pilot/identity.json -email a8@p2p.test
      -endpoint ${PILOT_SUBNET_PREFIX:-172.34.0}.28:4000 -listen :4000 -public -keepalive 5s -log-level info

  a9:
    build:
      context: ../..
      dockerfile: tests/integration/Dockerfile.multi
    hostname: a9
    depends_on:
      rendezvous:
        condition: service_healthy
    networks:
      pilot-p2p:
        ipv4_address: ${PILOT_SUBNET_PREFIX:-172.34.0}.29
    environment:
      PILOT_REGISTRY: "${PILOT_SUBNET_PREFIX:-172.34.0}.10:9000"
    command: >
      pilot-daemon
      -registry ${PILOT_SUBNET_PREFIX:-172.34.0}.10:9000
      -beacon ${PILOT_SUBNET_PREFIX:-172.34.0}.10:9001
      -hostname a9 -identity /root/.pilot/identity.json -email a9@p2p.test
      -endpoint ${PILOT_SUBNET_PREFIX:-172.34.0}.29:4000 -listen :4000 -public -keepalive 5s -log-level info

  a10:
    build:
      context: ../..
      dockerfile: tests/integration/Dockerfile.multi
    hostname: a10
    depends_on:
      rendezvous:
        condition: service_healthy
    networks:
      pilot-p2p:
        ipv4_address: ${PILOT_SUBNET_PREFIX:-172.34.0}.30
    environment:
      PILOT_REGISTRY: "${PILOT_SUBNET_PREFIX:-172.34.0}.10:9000"
    command: >
      pilot-daemon
      -registry ${PILOT_SUBNET_PREFIX:-172.34.0}.10:9000
      -beacon ${PILOT_SUBNET_PREFIX:-172.34.0}.10:9001
      -hostname a10 -identity /root/.pilot/identity.json -email a10@p2p.test
      -endpoint ${PILOT_SUBNET_PREFIX:-172.34.0}.30:4000 -listen :4000 -public -keepalive 5s -log-level info

networks:
  pilot-p2p:
    driver: bridge
    ipam:
      config:
        - subnet: ${PILOT_SUBNET_PREFIX:-172.34.0}.0/24
