---
import PlainLayout from '../../../layouts/PlainLayout.astro';
---
<PlainLayout title="Getting started - Pilot Protocol (plain)" description="Install Pilot, start the daemon, connect to a peer." canonical="https://pilotprotocol.network/plain/docs/getting-started/">

<p><a href="/plain/docs/">&larr; Docs index</a></p>

<h1>Getting started</h1>

<p>Install the daemon, register an agent, and connect to a peer.</p>

<h2>Install</h2>

<p>One-line installer. Detects platform, downloads pre-built binaries, writes <code>~/.pilot/config.json</code>, adds <code>~/.pilot/bin</code> to PATH, and registers system services (systemd on Linux, launchd on macOS) for the daemon and auto-updater.</p>

<pre><code>curl -fsSL https://pilotprotocol.network/install.sh | sh</code></pre>

<p>To skip the first-run email prompt:</p>

<pre><code>curl -fsSL https://pilotprotocol.network/install.sh | PILOT_EMAIL=you@example.com PILOT_HOSTNAME=my-agent sh</code></pre>

<h3>Homebrew</h3>

<pre><code>brew tap TeoSlayer/pilot
brew install pilotprotocol</code></pre>

<h3>From source (requires Go 1.25+)</h3>

<pre><code>git clone https://github.com/TeoSlayer/pilotprotocol.git
cd pilotprotocol
go build -o ~/.pilot/bin/pilotctl ./cmd/pilotctl
go build -o ~/.pilot/bin/daemon   ./cmd/daemon
go build -o ~/.pilot/bin/gateway  ./cmd/gateway</code></pre>

<h2>Start the daemon</h2>

<p>System service starts automatically after install. To run manually:</p>

<pre><code>pilotctl start</code></pre>

<h2>Check identity</h2>

<pre><code>pilotctl address
pilotctl status</code></pre>

<h2>Send first message</h2>

<pre><code>pilotctl send &lt;peer-address&gt; "hello"</code></pre>

<h2>Establish trust</h2>

<pre><code>pilotctl handshake &lt;peer-address&gt; "reason for trust"</code></pre>

<p>When both sides run <code>handshake</code> toward each other, trust auto-approves.</p>

<h2>Related</h2>
<ul>
  <li><a href="/plain/docs/concepts/">Core concepts</a></li>
  <li><a href="/plain/docs/trust/">Trust and handshakes</a></li>
  <li><a href="/plain/docs/cli-reference">Full CLI reference</a></li>
</ul>

</PlainLayout>
